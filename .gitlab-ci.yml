include:
  - local: 'ingress.yml'

ingress-example:
  extends: .combined-ingress
  variables:
    domain: example.org

cleanup:
  extends: .base_job
  script:
    - export KUBECONFIG=$USKUBECONFIG
    - kubectl delete namespace demo-shared-library
  tags:
    - iowa-c